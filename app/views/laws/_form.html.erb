<%= form_for(@law) do |f| %>
  <% if @law.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@law.errors.count, "error") %> prohibited this law from being saved:</h2>

      <ul>
      <% @law.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :ancestry %><br />
    <%= f.text_field :ancestry %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
