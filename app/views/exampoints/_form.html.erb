<%= form_for(@exampoint) do |f| %>
  <% if @exampoint.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@exampoint.errors.count, "error") %> prohibited this exampoint from being saved:</h2>

      <ul>
      <% @exampoint.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
